gatling {
  data {
    writers = [console, file]
    file {
      bufferSize = 8192
    }
  }
  http {
    warmUpUrl = "http://localhost:8080"
    enableGA = false
  }
  core {
    outputDirectoryBaseName = "danmakulive-gatling"
  }
}
